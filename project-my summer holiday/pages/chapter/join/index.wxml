<view class="join-page"><navigation-bar title="共同 Chapter" background="#F7F3EA"/><view class="join-card"><view class="invite-mark">＋</view><text class="eyebrow">SHARED CHAPTER</text><text class="join-title">一起把这段经历，<br/>记录完整。</text><text class="join-copy">加入后，你可以新增自己的 Moment，也可以补充朋友留下的片刻。</text><button class="primary-button" loading="{{joining}}" bindtap="join">{{joining ? '正在加入…' : '接受邀请'}}</button><text wx:if="{{error}}" class="error">{{error}}</text></view></view>
